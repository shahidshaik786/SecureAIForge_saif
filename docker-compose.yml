services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: saif
      POSTGRES_PASSWORD: saifpass
      POSTGRES_DB: saif_db
    ports:
      - "5433:5432"
    volumes:
      - saif_postgres:/var/lib/postgresql/data

volumes:
  saif_postgres:
